{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Shared compiler options for the monorepo. Created in Phase 1 as a skeleton; the per-app and per-package tsconfigs will extend this in Phases 2-4 (see MONOREPO-MIGRATION-PLAN.md). The existing app's tsconfig.json is left untouched in Phase 1.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true
  }
}
